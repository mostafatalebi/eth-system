## Ethereum Simulator & Indexer
This is a rust project that enables you to simulate
transaction executions or index the blockchain.

Currently only simulator is done. 

`./eth_app --mode simulator --config example.env`




`./eth_app --mode simulator --config example.env`


